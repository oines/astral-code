name: sdk (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "SDK CI is intentionally disabled until Astral SDK packaging is configured."
