name: Issue Labeler (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "Issue labeling is intentionally disabled until Astral automation is configured."
