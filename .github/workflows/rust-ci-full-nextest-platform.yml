name: rust-ci-full nextest platform (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reusable nextest platform CI is intentionally disabled until Astral CI is configured."
