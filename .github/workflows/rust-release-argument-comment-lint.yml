name: rust-release-argument-comment-lint (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "Argument comment lint release assets are intentionally disabled until Astral releases are configured."
