name: rust-release-zsh (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "zsh release artifact builds are intentionally disabled until Astral releases are configured."
