name: rust-release-prepare (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Disabled during Astral migration
    runs-on: ubuntu-latest
    steps:
      - run: echo "Rust release preparation is intentionally disabled until Astral releases are configured."
