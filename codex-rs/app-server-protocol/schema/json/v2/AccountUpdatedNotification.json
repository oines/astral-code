{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AuthMode": {
      "description": "Authentication mode for Astral-managed credentials.",
      "oneOf": [
        {
          "description": "Provider API key supplied by the caller and stored locally.",
          "enum": [
            "apikey"
          ],
          "type": "string"
        }
      ]
    }
  },
  "properties": {
    "authMode": {
      "anyOf": [
        {
          "$ref": "#/definitions/AuthMode"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "title": "AccountUpdatedNotification",
  "type": "object"
}