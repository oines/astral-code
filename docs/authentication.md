# Authentication

Astral authentication is provider-neutral. Use `ASTRAL_API_KEY`,
`ASTRAL_BASE_URL`, and provider entries in `config.toml`; hosted login state
from the upstream project is not part of the Astral bootstrap path.
