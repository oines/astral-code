# Getting started with Astral

Astral is a provider-neutral coding agent harness. Build the CLI with:

```bash
cargo build --manifest-path codex-rs/Cargo.toml -p codex-cli --bin astral
```

Then run:

```bash
./codex-rs/target/debug/astral
```
